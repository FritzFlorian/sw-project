package com.ecorp.fritzshipping.entity;

/**
 * Describes different types of stops that an package
 * can be handed over during it's delivery.
 */
public enum TrackingType {
    PICKUP,
    HAND_IN,
    PACKAGE_CENTER,
    DELIVERY
}
